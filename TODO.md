TODO:

* built-in functions:
   - concat array
   - slice array
* integer to string | string to integer -> parse (one function)
* standard lib: map, filter etc.
* errors
* cli

# perhaps
* while
* for
* else

